# Backend социальной сети для обмена фотографиями

> Дипломная работа профессии «Python-разработчик с нуля» Netology
> Реализация API для публикации постов с изображениями, лайками и комментариями.  
> Поддержка нескольких изображений и геолокации поста.

---

## Цель проекта

Разработать **backend** приложения для социальной сети, где пользователи могут:

- ✅ Загружать текстовые публикации с фотографией
- ✅ Комментировать и ставить лайки другим публикациям
- ✅ Редактировать/удалять свои посты
- ✅ Получать детали поста, включая список комментариев и количество лайков
- ✅ Загружать несколько изображений к одному посту *(дополнительное задание)*
- ✅ Указывать локацию при создании поста и получать обратно название места по координатам *(дополнительное задание)*

---

## Технологии

- Python 3.x
- Django + Django REST Framework
- PostgreSQL
- DRF TokenAuthentication
- geopy
- drf-spectacular

---

## Структура проекта

### Модели

#### `Post`
- `author`: Автор поста
- `text`: Текст поста
- `image`: Изображение (или связь с `PostImage`)
- `created_at`: Дата создания
- `location`: Адрес, указанный пользователем
- `latitude`, `longitude`: Координаты, вычисляемые автоматически на основе `location`

#### `Comment`
- `author`: Автор комментария
- `post`: Ссылка на пост
- `text`: Текст комментария
- `created_at`: Дата создания

#### `Like`
- `author`: Кто поставил лайк
- `post`: Какой пост был оценён

#### `PostImage` *(дополнительно)*
- `post`: Связь с постом
- `image`: Фото пользователя

---

## 📦 Эндпоинты

| Метод | URL                         | Описание |
|-------|------------------------------|----------|
| GET   | `/api/posts/`                | Получить список всех постов |
| POST  | `/api/posts/`                | Создать новый пост (только авторизованный) |
| GET   | `/api/posts/{id}/`           | Получить детали конкретного поста |
| PUT/PATCH | `/api/posts/{id}/`         | Обновить пост (только автор) |
| DELETE | `/api/posts/{id}/`          | Удалить пост (только автор) |
| POST  | `/api/posts/{id}/comment/`   | Оставить комментарий (только авторизованный) |
| POST  | `/api/posts/{id}/like/`      | Поставить или убрать лайк (только авторизованный) |
| POST  | `/api/posts/{id}/images/`    | Загрузить несколько изображений к посту *(доп. задание)* |
| DELETE| `/api/posts/{id}/images/`    | Удалить все изображения поста *(доп. задание)* |

---

## 📝 Пример ответа API

```json
{
  "id": 1,
  "text": "Новый прекрасный день",
  "image": "/media/posts/image1.jpg",
  "created_at": "2024-02-23T02:24:29.338414Z",
  "comments": [
    {
      "author": 2,
      "text": "Круто",
      "created_at": "2024-02-23T05:12:31.054234Z"
    }
  ],
  "likes_count": 20
}
```

---

## 🚀 Дополнительные функции

### 1. **Загрузка нескольких изображений**

- Можно загрузить несколько файлов одним запросом.
- Все изображения привязываются к одному посту.
- Поддержка удаления одного или всех изображений.

### 2. **Геоданные поста**

- При создании поста можно указать адрес (`location`).
- По этому адресу система автоматически получает широту и долготу через `geopy.geocode()`.
- При выводе данных — выполняется `reverse()` и отображается реальное название объекта по координатам.

Пример:
```python
location = "Москва, Россия" → latitude = 55.755826, longitude = 37.6173
```

---

## 🧰 Установка и запуск

### 1. Клонируй репозиторий

```bash
git clone https://github.com/yourname/social-network-api.git
cd social-network-api
```

### 2. Создай виртуальное окружение и установи зависимости

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate   # Windows

pip install -r requirements.txt
```

### 3. Настрой БД в `settings.py`

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'netology_diplom',
        'USER': 'postgres',
        'PASSWORD': '1',
        'HOST': 'localhost',
        'PORT': '5432'
    }
}
```

### 4. Выполни миграции

```bash
python manage.py migrate
```

### 5. Запусти сервер

```bash
python manage.py runserver
```

---

## 📚 Документация API

После запуска сервера открой:

- Swagger: http://127.0.0.1:8000/api/swagger/
- ReDoc: http://127.0.0.1:8000/api/redoc/

Там ты найдёшь полную документацию всех эндпоинтов.

---

## 📑 Проверка работоспособности

### Примеры запросов:

#### Создать пост
```http
POST {{baseUrl}}/posts/
Authorization: Token <your_token>
Content-Type: multipart/form-data

text: Новый пост
image: <file.jpg>
```

#### Получить пост с комментариями и лайками
```http
GET {{baseUrl}}/posts/1/
```

#### Оставить комментарий
```http
POST {{baseUrl}}/posts/1/comment/
Authorization: Token <your_token>
Content-Type: application/json

{
  "text": "Классный пост!"
}
```

#### Поставить лайк
```http
POST {{baseUrl}}/posts/1/like/
Authorization: Token <your_token>
```

---

## 🛠 Возможности для расширения

- Добавление поддержки Google Maps вместо Nominatim
- Кэширование результатов геокодирования
- Предпросмотр постов на карте
- Отправка push-уведомлений о новых лайках и комментариях
- Добавление подписок и фаворитов

---

## 📄 Лицензия

MIT License — свободно используй и модифицируй!

---

## 🙌 Благодарность

Спасибо за выполнение дипломного проекта!  
Это отличное приложение для портфолио, которое демонстрирует навыки:

- Работа с базами данных и ORM
- Проектирование API
- Использование сериализаторов и ViewSet'ов
- Работа с медиафайлами и геоданными

---

Если хочешь, я могу сделать:

- ✅ README на английском
- ✅ Readme с иконками, шаблоном GitHub и примерами curl
- ✅ CI/CD скрипты
- ✅ Dockerfile и docker-compose.yml для развёртки

Хочешь? 😊